# WB Redistribution Bot

Telegram-бот для управления остатками и перераспределения товаров между складами Wildberries.

## Возможности

- **🆕 Telegram Mini App** для управления перераспределением остатков
- Поддержка нескольких поставщиков (мультиаккаунт)
- Мониторинг коэффициентов приёмки в реальном времени
- Автобронирование слотов при появлении выгодных коэффициентов
- Уведомления об изменениях коэффициентов
- Рекомендации "куда везти" на основе текущих коэффициентов

## Требования

- Python 3.10+
- Telegram Bot Token (от @BotFather)
- WB API Token (из ЛК Wildberries)

## Установка

### 1. Клонируйте репозиторий

```bash
git clone <repo-url>
cd wbpereraspr
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте окружение

Скопируйте файл конфигурации:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Telegram Bot Token (получить у @BotFather)
BOT_TOKEN=your_bot_token_here

# Ваш Telegram ID (получить у @userinfobot)
ADMIN_IDS=123456789

# Ключ шифрования для токенов (сгенерируйте командой ниже)
WB_ENCRYPTION_KEY=your_encryption_key
```

### 4. Сгенерируйте ключ шифрования

```bash
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

Скопируйте результат в `WB_ENCRYPTION_KEY` в файле `.env`.

### 5. Настройте WEBAPP_URL

Для локальной разработки в `.env`:

```env
WEBAPP_URL=http://localhost:8080
```

Для продакшена укажите свой домен:

```env
WEBAPP_URL=https://your-domain.com
```

## Запуск

### Запуск бота + API (рекомендуется)

```bash
python run.py
```

Запустится:
- Telegram бот
- FastAPI сервер на порту 8080
- Mini App доступен по адресу http://localhost:8080/webapp

### Запуск только бота (без Mini App)

```bash
python bot.py
```

### Отдельный запуск API

```bash
uvicorn api.main:app --host 0.0.0.0 --port 8080
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы + кнопка открытия Mini App |
| `/token` | Добавить/удалить WB API токен |
| `/suppliers` | Управление поставщиками |
| `/redistribute` | **Перераспределение остатков** → открывает Mini App |
| `/help` | Справка |

> **📝 Примечание**: Команда `/redistribute` открывает визуальную форму в Mini App для удобного создания заявок. Старый текстовый интерфейс (FSM) был удалён для упрощения UX. Подробнее: [docs/INTERFACE_UNIFICATION.md](docs/INTERFACE_UNIFICATION.md)

## Получение WB API токена

1. Перейдите в [ЛК Wildberries → Настройки → Доступ к API](https://seller.wildberries.ru/supplier-settings/access-to-api)
2. Нажмите "Создать токен" → "Для интеграции вручную"
3. Выберите тип: **Базовый токен**
4. Отметьте категории:
   - Маркетплейс
   - Поставки
   - Контент (для получения информации о товарах)
5. Уровень доступа: **Чтение и запись**
6. Скопируйте токен и отправьте боту через `/token`

## Структура проекта

```
wbpereraspr/
├── bot.py                 # Главный файл бота
├── run.py                 # Запуск бота + API
├── config.py              # Конфигурация
├── database.py            # База данных SQLite
│
├── api/                   # 🆕 FastAPI backend
│   ├── main.py            # FastAPI приложение
│   ├── auth.py            # Валидация Telegram WebApp
│   └── routes/            # API endpoints
│       ├── suppliers.py   # Управление поставщиками
│       ├── products.py    # Поиск товаров
│       ├── stocks.py      # Остатки
│       ├── warehouses.py  # Склады
│       └── requests.py    # Заявки на перемещение
│
├── webapp/                # 🆕 Telegram Mini App
│   ├── index.html
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── app.js
│
├── handlers/              # Telegram handlers
│   ├── token_management.py
│   ├── supplier_management.py
│   └── redistribution.py
│
├── services/              # Бизнес-логика
│   └── redistribution_service.py
│
├── wb_api/                # WB API клиенты
│   ├── client.py          # HTTP клиент с rate limiting
│   ├── stocks.py          # API остатков
│   ├── supplies.py        # API поставок
│   └── warehouses.py      # API складов
│
└── utils/
    └── encryption.py      # Шифрование токенов
```

## Telegram Mini App

Mini App для управления перераспределением остатков между складами.

### Функционал

1. **Выбор поставщика** - поддержка нескольких ИП/ООО
2. **Поиск товара** - по артикулу WB (от 3 цифр)
3. **Выбор склада-источника** - показывает только склады с остатками
4. **Указание количества** - с валидацией максимума
5. **Выбор склада-назначения** - все доступные склады WB
6. **Управление заявками**:
   - Табы "Текущие" / "Архив"
   - Редактирование количества
   - Удаление заявок

### Открытие Mini App

1. Отправьте команду `/start` боту
2. Нажмите кнопку "📦 Открыть Перераспределение"
3. Mini App откроется внутри Telegram

### API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/suppliers` | GET | Список поставщиков |
| `/api/products/search?q={nmId}` | GET | Поиск товара |
| `/api/stocks/{nmId}` | GET | Остатки по складам |
| `/api/warehouses` | GET | Список складов |
| `/api/requests` | GET | Заявки пользователя |
| `/api/requests` | POST | Создать заявку |
| `/api/requests/{id}` | PATCH | Обновить заявку |
| `/api/requests/{id}` | DELETE | Удалить заявку |

Документация API: http://localhost:8080/docs

## Лицензия

MIT
