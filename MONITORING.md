# 📊 Руководство по управлению мониторингом

## Обзор

Система мониторинга отслеживает изменения коэффициентов приёмки Wildberries и уведомляет пользователей о выгодных слотах.

---

## 🎛 Способы управления мониторингом

### 1. Глобальное отключение (через .env)

Полностью выключить мониторинг для всех пользователей:

```bash
# В файле .env
ENABLE_MONITORING=false
```

После изменения перезапустите бота:
```bash
python3 run.py
```

**Когда использовать**:
- Техническое обслуживание
- Превышение rate limit WB API
- Временное отключение функции

---

### 2. Управление через бот (команда /monitor)

#### Создать подписку
1. `/monitor` → выбрать "Настроить мониторинг"
2. Выбрать склады (чекбоксы)
3. Выбрать коэффициенты (0, 0.5, 1, 1.5, 2)
4. Включить/выключить автобронирование

#### Просмотр подписок
`/monitor` → показывает все активные подписки

#### Управление подписками
`/monitor` → "Изменить подписки" → выбрать подписку:
- ⏸ **Приостановить** - временно отключить уведомления
- ▶️ **Возобновить** - включить снова
- 🗑 **Удалить** - удалить подписку навсегда

---

## ⚙️ Настройки фильтрации

### Cooldown уведомлений

Предотвращает спам при частых изменениях коэффициентов:

```bash
# В файле .env
NOTIFICATION_COOLDOWN_MINUTES=10
```

**Как работает**:
- После отправки уведомления о складе "Коледино"
- Следующее уведомление по этому складу не раньше чем через 10 минут
- Для других складов cooldown не действует

**Рекомендуемые значения**:
- `5` - для активного мониторинга (больше уведомлений)
- `10` - оптимально (по умолчанию)
- `15-20` - для минимизации уведомлений

---

### Фильтр значимых изменений

Бот уведомляет **только** когда:

1. 🆓 Коэффициент стал **0** (бесплатно!)
2. 🔥 Коэффициент стал **≤ 0.5** (очень выгодно)
3. ✅ Склад стал **доступен** (было -1, стало ≥ 0)
4. 📊 Изменение **≥ 0.2** (значимое)

**Не уведомляет** о мелких колебаниях типа 1.2 → 1.25

Настройка в коде: [bot.py:67-72](bot.py#L67-L72)

---

## 🔧 Параметры конфигурации

### Основные параметры (.env)

```bash
# Интервал опроса WB API (секунды)
# Оптимально: 12 сек = 5 req/min (83% от лимита)
COEFFICIENT_POLL_INTERVAL=12

# Rate limit WB API
COEFFICIENT_RATE_LIMIT=6

# Cooldown уведомлений (минуты)
NOTIFICATION_COOLDOWN_MINUTES=10

# Глобальное включение/выключение
ENABLE_MONITORING=true

# Системный токен для глобального мониторинга
WB_SYSTEM_TOKEN=your_wb_token_here
```

---

## 📱 Команды бота

| Команда | Описание |
|---------|----------|
| `/monitor` | Настройка мониторинга складов |
| `/coefficients` | Текущие коэффициенты всех складов |
| `/recommend` | Рекомендации куда везти товар |
| `/book` | Ручное бронирование слота |
| `/history` | История бронирований |

---

## 🔍 Диагностика

### Проверить работает ли мониторинг

1. Проверить логи после запуска бота:
```bash
python3 run.py
```

Должно быть:
```
INFO - Starting GLOBAL monitoring with system token
INFO - Coefficient monitor started (interval: 12s)
```

2. Если видите:
```
INFO - Monitoring is DISABLED in config
```
Значит `ENABLE_MONITORING=false` в .env

3. Если видите:
```
WARNING - WB_SYSTEM_TOKEN not configured
```
Нужно добавить WB токен в .env

### Проверить подписки пользователя

Открыть базу данных:
```bash
sqlite3 bot_data.db
SELECT * FROM monitoring_subscriptions WHERE user_id = YOUR_USER_ID;
```

Поля:
- `is_active` - 1 = активна, 0 = отключена
- `warehouse_ids` - JSON массив ID складов
- `target_coefficients` - JSON массив коэффициентов
- `auto_book` - 1 = автобронирование включено

---

## 🚨 Частые проблемы

### Проблема: Получаю слишком много уведомлений

**Решение**:
1. Увеличить `NOTIFICATION_COOLDOWN_MINUTES` до 15-20
2. Уменьшить количество отслеживаемых складов
3. Выбрать только критичные коэффициенты (0, 0.5)

### Проблема: Не получаю уведомления

**Чеклист**:
- [ ] `ENABLE_MONITORING=true` в .env
- [ ] `WB_SYSTEM_TOKEN` настроен
- [ ] Бот запущен и не упал
- [ ] Есть активная подписка (`/monitor`)
- [ ] Подписка включает нужные склады
- [ ] Коэффициент изменился значимо (см. фильтры)
- [ ] Не действует cooldown (прошло > 10 минут)

### Проблема: Мониторинг не запускается

**Причины**:
1. Невалидный WB токен → проверить токен в ЛК WB
2. Rate limit превышен → увеличить `COEFFICIENT_POLL_INTERVAL`
3. БД не создана → запустить бота для инициализации

---

## 📊 Статистика мониторинга

Для администраторов (команда `/stats`):
- Количество опросов WB API
- Обнаруженных изменений
- Размер кэша коэффициентов
- Статус мониторинга

---

## 🔐 Безопасность

1. **Токен WB** хранится зашифрованным (Fernet)
2. **Cooldown** защищает от спама
3. **Rate limiting** предотвращает блокировку от WB API
4. **Фильтры** снижают нагрузку на Telegram API

---

## 💡 Рекомендации

### Для начинающих
- Начните с 2-3 складов
- Выберите коэффициенты: 0, 0.5, 1
- Отключите автобронирование (изучите сначала)

### Для опытных
- Используйте автобронирование для коэффициента 0
- Мониторьте 5-8 складов в разных регионах
- Настройте cooldown под свои нужды

### Для масштабирования (> 100 пользователей)
- Переход на Redis для кэширования
- Celery для распределенных задач
- PostgreSQL вместо SQLite
- Отдельные воркеры для мониторинга

---

## 📚 Дополнительные материалы

- [План архитектурных улучшений](/Users/producer/.claude/plans/lucky-orbiting-summit.md)
- [Документация WB API](https://openapi.wildberries.ru/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Версия документа**: 1.0
**Последнее обновление**: 2026-01-27
