# 🔍 Руководство по диагностике проблемы "Бот молчит"

## Проблема
После ввода названия магазина бот не отправляет финальное сообщение с кнопкой Mini App.

## Шаги диагностики

### ШАГ 1: Проверить переменные окружения на Railway

**Критично важно**: WEBAPP_URL должен быть установлен на HTTPS URL вашего Railway приложения!

1. Откройте Railway Dashboard
2. Перейдите в ваш проект → Variables
3. Проверьте переменную `WEBAPP_URL`

**❌ НЕПРАВИЛЬНО (не будет работать)**:
```
WEBAPP_URL=http://localhost:8080
```

**✅ ПРАВИЛЬНО**:
```
WEBAPP_URL=https://your-app-name.up.railway.app
```

> **Почему это важно**: Telegram требует HTTPS URL для Mini App в продакшене. Если указан localhost, WebAppInfo будет отклонен Telegram API и сообщение не отправится.

---

### ШАГ 2: Найти Railway URL

Если вы не знаете свой Railway URL:

1. Откройте Railway Dashboard
2. Перейдите в ваш проект
3. Во вкладке **Settings** найдите раздел **Domains**
4. Скопируйте URL (будет вида: `https://xxxxx.up.railway.app`)

---

### ШАГ 3: Обновить WEBAPP_URL на Railway

1. Railway Dashboard → Variables
2. Найдите `WEBAPP_URL`
3. Нажмите на переменную для редактирования
4. Замените значение на ваш Railway URL (например: `https://wbbot-production.up.railway.app`)
5. Сохраните изменения

**Railway автоматически перезапустит деплоймент** с новой переменной.

---

### ШАГ 4: Проверить логи после изменения

После обновления WEBAPP_URL и перезапуска:

1. Откройте логи Railway (вкладка **Deployments** → **View Logs**)
2. Дождитесь сообщения: `✅ Бот успешно запущен!`
3. В Telegram отправьте боту:
   - `/start`
   - Отправьте токен
   - Введите название магазина (например: "iphasnulin")

4. **Сразу** обновите страницу с логами Railway

5. Найдите в логах следующие строки (мы их добавили для отладки):

```
INFO - Token added successfully: token_id=X, name=iphasnulin
INFO - Supplier added: supplier_id=Y
INFO - WEBAPP_URL from config: https://your-app.up.railway.app
INFO - Full Mini App URL: https://your-app.up.railway.app/webapp/index.html
INFO - Sending message with Mini App button...
INFO - Message sent successfully!
```

---

### ШАГ 5: Интерпретация результатов

#### ✅ Если видите "Message sent successfully!"
- **Проблема решена!** Бот должен отправить сообщение с кнопкой Mini App
- Проверьте Telegram - должно прийти сообщение с кнопкой "📦 Открыть Перераспределение"

#### ❌ Если видите "Failed to send message with Mini App button: [error]"
- **Ошибка найдена!** Скопируйте полный текст ошибки из логов
- Ошибка покажет точную причину (например: Invalid WebApp URL, HTTPS required и т.д.)

#### ⚠️ Если НЕ видите наших debug сообщений вообще
- **Возможные причины**:
  1. Код не обновился на Railway (проверьте последний коммит в Deployments)
  2. Бот использует старую версию кода
  3. Логи не обновились (попробуйте F5 на странице логов)

---

### ШАГ 6: Локальная проверка (опционально)

Можно также проверить переменные локально:

```bash
cd /Users/producer/Documents/Projects/wbpereraspr
python check_env.py
```

Этот скрипт покажет все переменные окружения и предупредит о проблемах.

---

## Типичные ошибки и решения

### Ошибка 1: "Invalid WebApp URL"
**Причина**: WEBAPP_URL не является валидным HTTPS URL
**Решение**: Установите `WEBAPP_URL=https://your-app.up.railway.app`

### Ошибка 2: "Bad Request: BUTTON_URL_INVALID"
**Причина**: URL кнопки Mini App невалиден (например, localhost в продакшене)
**Решение**: Используйте HTTPS URL Railway приложения

### Ошибка 3: Бот всё равно молчит
**Причина**: Возможно, ошибка происходит раньше (при добавлении supplier)
**Решение**:
- Проверьте логи на наличие `Failed to add supplier: [error]`
- Если есть такая ошибка - это проблема с базой данных
- Возможно, нужно пересоздать таблицу suppliers

---

## Быстрая проверка (checklist)

Перед тестированием убедитесь:

- [ ] Railway деплоймент запущен (статус: Active)
- [ ] Переменная `WEBAPP_URL` установлена на HTTPS URL Railway
- [ ] Переменная `BOT_TOKEN` корректна
- [ ] Переменная `WB_ENCRYPTION_KEY` установлена
- [ ] В логах есть сообщение: `✅ Бот успешно запущен!`
- [ ] Локальные боты остановлены (pkill -f "python.*run")

---

## Что делать дальше

### Если проблема решена
1. Протестируйте полный flow: `/start` → добавить токен → создать заявку в Mini App
2. Убедитесь что Mini App открывается корректно
3. Можно переходить к следующему этапу разработки

### Если проблема НЕ решена
1. Скопируйте ВСЕ логи Railway после добавления токена
2. Скопируйте текст ошибки (если есть)
3. Проверьте переменные окружения на Railway (скриншот)
4. Отправьте всю эту информацию

---

**Обновлено**: 2026-01-27
**Версия**: 1.0
