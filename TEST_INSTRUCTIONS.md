# 🧪 Инструкция по тестированию автоматического перераспределения

## Предварительные требования

1. ✅ Авторизация через SMS уже прошла (есть сессия в БД)
2. ✅ Есть тестовый артикул с остатками на складе
3. ✅ Известны ID складов (источник и назначение)
4. ✅ На целевом складе есть квота для приёма

## Быстрый старт

### Шаг 1: Подготовка данных

Вам понадобится:
- **Номер телефона**: тот же, что использовался для авторизации через /start
- **Артикул (nm_id)**: любой артикул с остатками (например, из вашего каталога)
- **ID складов**: можно посмотреть в ЛК Wildberries или через API

**Где взять ID складов:**
```python
# Вариант 1: Из Mini App - откройте webapp и посмотрите в списке складов
# Вариант 2: Через WB API
from wb_api.warehouses import WBWarehousesClient
client = WBWarehousesClient("your_token")
warehouses = client.get_warehouses()
for wh in warehouses:
    print(f"{wh['name']}: {wh['id']}")
```

### Шаг 2: Запуск теста с визуальным контролем (рекомендуется для первого раза)

```bash
cd /Users/producer/Documents/Projects/wbpereraspr

python test_redistribution.py \
  --phone "+79991234567" \
  --nm-id 123456789 \
  --from-warehouse 1 \
  --to-warehouse 2 \
  --quantity 1 \
  --visible
```

**Флаг `--visible`** откроет браузер с GUI - вы увидите, как бот:
1. Открывает seller.wildberries.ru
2. Переходит на страницу перераспределения
3. Ищет артикул
4. Выбирает склады
5. Вводит количество
6. Подтверждает перемещение

### Шаг 3: Анализ результата

#### ✅ Успех
```
РЕЗУЛЬТАТ
================================================================================
Статус: success
Сообщение: Заявка на перемещение создана
ID заявки: 123456789
Скриншот сохранён: screenshots/test_redistribution_123456789.png
================================================================================

✅ УСПЕХ! Заявка на перемещение создана
📝 Следующий шаг: Проверьте заявку в ЛК Wildberries
💡 Совет: Если всё работает, можно переходить к интеграции с Mini App
```

**Что делать дальше:**
- Проверить заявку в ЛК Wildberries
- Переходить к Фазе 3 (интеграция с Mini App)

#### ⚠️ Нужно уточнить селекторы

```
Статус: error
Сообщение: Не удалось найти поле ввода артикула
Скриншот сохранён: screenshots/test_redistribution_123456789.png
```

**Что делать:**
1. Открыть скриншот `screenshots/test_redistribution_*.png`
2. Посмотреть на какой странице остановился бот
3. Перейти к Фазе 2 - уточнение селекторов (см. ниже)

#### ⏰ Сессия истекла

```
Статус: session_expired
Сообщение: Сессия истекла. Необходима повторная авторизация.
```

**Что делать:**
- Повторно авторизоваться через `/start` в Telegram боте
- Запустить тест снова

#### 🚫 Нет квоты

```
Статус: no_quota
Сообщение: Нет квоты на выбранном направлении
```

**Что делать:**
- Попробовать другой целевой склад (--to-warehouse)
- Дождаться появления квоты
- Попробуйте позже, когда квота появится

## Фаза 2: Уточнение селекторов (если нужно)

Если тест показал ошибку связанную с селекторами, нужно их уточнить:

### Шаг 1: Открыть DevTools

1. Откройте Chrome
2. Перейдите на https://seller.wildberries.ru
3. Авторизуйтесь
4. Перейдите на страницу перераспределения: `/supplies-management/redistribution`
5. Нажмите F12 (DevTools)

### Шаг 2: Найти селекторы

Нажмите Ctrl+Shift+C (инспектор элементов) и кликните на:

1. **Поле ввода артикула** - запишите селектор (например, `input[name="article"]`)
2. **Кнопка поиска** - запишите селектор
3. **Dropdown склада-источника** - запишите селектор
4. **Dropdown склада-назначения** - запишите селектор
5. **Поле количества** - запишите селектор
6. **Кнопка "Создать"/"Переместить"** - запишите селектор

### Шаг 3: Обновить селекторы

Откройте файл [browser/redistribution.py:53-74](browser/redistribution.py#L53-L74):

```python
SELECTORS = {
    # Замените на реальные селекторы из DevTools
    'article_input': 'input[placeholder*="артикул"]',  # ← обновите
    'article_search': 'button:has-text("Найти")',      # ← обновите
    'source_select': 'select[name="source"]',           # ← обновите
    'target_select': 'select[name="target"]',           # ← обновите
    'quantity_input': 'input[type="number"]',           # ← обновите
    'submit_button': 'button[type="submit"]',           # ← обновите
    'confirm_button': 'button:has-text("Подтвердить")', # ← обновите
    'success_message': '[class*="success"]',            # ← обновите
    'error_message': '[class*="error"]',                # ← обновите
}
```

### Шаг 4: Повторный тест

Запустите test_redistribution.py снова с обновлёнными селекторами.

## Запуск в headless режиме (после успешных тестов)

После того как тест прошёл с `--visible`, можно запускать без GUI:

```bash
python test_redistribution.py \
  --phone "+79991234567" \
  --nm-id 123456789 \
  --from-warehouse 1 \
  --to-warehouse 2 \
  --quantity 1
```

## Логи

Все логи сохраняются в:
- `test_redistribution.log` - детальные логи выполнения
- `screenshots/` - скриншоты страниц при ошибках

## Частые проблемы

### Проблема: "Сессия не найдена"

**Решение:**
1. Проверьте формат номера телефона (должен быть с +, например +79991234567)
2. Убедитесь что авторизовались через /start в боте
3. Проверьте БД: `sqlite3 bot_data.db "SELECT * FROM browser_sessions"`

### Проблема: "Playwright not installed"

**Решение:**
```bash
pip install playwright
playwright install chromium
```

### Проблема: "Timeout waiting for selector"

**Решение:**
- Страница загружается медленно → увеличьте timeout в browser_service.py
- Селекторы неправильные → см. Фазу 2 выше

## Следующие шаги

После успешного теста:

1. ✅ **Фаза 3**: Создать API endpoint `/api/redistribution/execute`
2. ✅ **Фаза 3**: Добавить кнопку "🤖 Автоматически" в Mini App
3. ✅ **Фаза 4**: Добавить обработку edge cases
4. ✅ **Фаза 5**: Тестирование всех сценариев
5. ✅ **Деплой**: Push на Railway

---

## Пример реального запуска

```bash
# Замените на ваши реальные данные:
python test_redistribution.py \
  --phone "+79161234567" \
  --nm-id 123456789 \
  --from-warehouse 507 \      # Коледино
  --to-warehouse 1733 \        # Электросталь
  --quantity 2 \
  --visible
```

Удачи! 🚀
