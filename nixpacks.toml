[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = [
    "pip install -r requirements.txt --break-system-packages",
    "playwright install --with-deps chromium"
]

[start]
cmd = "python bot.py"
