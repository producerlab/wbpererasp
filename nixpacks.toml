[phases.setup]
# Не используем aptPkgs - Railway не всегда их поддерживает правильно

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "pip install playwright",
    "playwright install chromium"
]

[start]
cmd = "python3 bot.py"
